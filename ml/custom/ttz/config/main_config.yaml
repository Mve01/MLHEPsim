defaults:
  - _self_
  - data_config
  - training_config
  - experiment_config
  - maf_made_mog@model_config
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

hydra:
  output_subdir: null
  run:
    dir: .
